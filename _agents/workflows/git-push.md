---
description: Auto-commit and push changes to git
---

# Git Push Workflow
Run these steps whenever the user requests to automatically push changes.

// turbo-all
1. Add all changes, commit with the provided message, and push to origin.
